{
  "name": "zuik",
  "version": "1.0.0",
  "private": true,
  "description": "Intent-based DeFi automation platform on Algorand",
  "scripts": {
    "build": "cd projects/Zuik-frontend && npm install && npm run build",
    "dev": "cd projects/Zuik-frontend && npm run dev"
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "@types/node": "^20.0.0"
  }
}