[algokit]
min_version = "v2.0.0"

[project]
type = 'workspace'
projects_root_path = 'projects'

[project.run]
# Zuik-contracts removed from repo; frontend is the active build target
build = ['Zuik-frontend']
